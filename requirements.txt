# Core runtime
pydantic>=1.10
httpx>=0.24
beautifulsoup4>=4.12
Jinja2>=3.1
PyYAML>=6.0
python-dotenv>=1.0

# Optional HTML → PDF engine (choose one in runtime config)
# weasyprint>=60.0
# (or install the wkhtmltopdf binary via your OS package manager)
